<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.dumpram.sbm"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="21" />

    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="21" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="21" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_PRIVILEGED"
        android:maxSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".TerminalActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme" >
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/FullscreenTheme" >
        </activity>
        <activity
            android:name=".StartupActivity"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HelpActivity"
            android:label="@string/title_activity_help"
            android:theme="@style/FullscreenTheme" >
        </activity>
        <activity
            android:name=".PairActivity"
            android:label="@string/title_activity_pair"
            android:theme="@style/FullscreenTheme" >
        </activity>
        <activity
            android:name=".ConnectActivity"
            android:label="@string/title_activity_connect"
            android:theme="@style/FullscreenTheme" >
        </activity>
        <activity
            android:name=".BluetoothSettingsActivity"
            android:label="@string/title_activity_bluetooth_settings"
            android:theme="@style/FullscreenTheme" >
        </activity>
        <activity
            android:name=".WiFlySettingsActivity"
            android:label="@string/title_activity_wi_fly_settings" 
            android:theme="@style/FullscreenTheme">
        </activity>
        <activity
            android:name=".RTCSettingsActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_rtcsettings"
            android:theme="@style/FullscreenTheme" >
        </activity>
    </application>

</manifest>
